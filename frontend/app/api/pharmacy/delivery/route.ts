import { gone } from '@/lib/gone'

export async function GET() {
  return gone('Pharmacy delivery APIs have been retired. Use admin Apollo fulfilment.')
}

export async function PATCH() {
  return gone('Pharmacy delivery APIs have been retired. Use admin Apollo fulfilment.')
}

export async function POST() {
  return gone('Pharmacy delivery APIs have been retired. Use admin Apollo fulfilment.')
}
